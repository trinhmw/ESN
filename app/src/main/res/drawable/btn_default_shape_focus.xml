<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <padding
        android:left="@dimen/btn_padding"
        android:top="@dimen/btn_padding"
        android:right="@dimen/btn_padding"
        android:bottom="@dimen/btn_padding" />
    <corners
        android:radius="@dimen/btn_radius" />
    <stroke
        android:color="@color/btn_focus_stroke"
        android:width="@dimen/btn_stroke" />

    <gradient
    android:startColor="@color/seat_unavail_start"
    android:endColor="@color/seat_unavail_end"
    android:angle="90"
    />

</shape>