<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <padding
        android:left="@dimen/btn_padding"
        android:top="@dimen/btn_padding"
        android:right="@dimen/btn_padding"
        android:bottom="@dimen/btn_padding" />
    <corners
        android:radius="@dimen/btn_radius" />
    <stroke
        android:color="@color/btn_press_stroke"
        android:width="1dp" />

    <gradient
    android:startColor="@color/btn_press_start"
    android:endColor="@color/btn_press_end"
    android:angle="@integer/btn_angle"
    />
</shape>